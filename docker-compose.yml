version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./reports:/app/reports
    environment:
      - OLLAMA_HOST=http://localhost:11434
      - UV_SYSTEM_PYTHON=1
    restart: unless-stopped